name: Integrate Long Capture Next v12

on:
  push:
    branches:
      - feature/long-capture-next-v12

permissions:
  contents: write

jobs:
  integrate:
    runs-on: windows-latest
    steps:
      - name: Checkout StarCap
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Materialize v12 source changes
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $patch = 'tools\patch_long_capture_next_v12.py'
          if (-not (Test-Path $patch)) {
            Write-Host 'v12 source changes already materialized.'
            exit 0
          }
          python $patch
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          Remove-Item $patch -Force
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Src/Win/CapLong.cpp Src/Win/CapLong.h tools/patch_long_capture_next_v12.py
          git commit -m 'Serialize long capture steps without v11 motion gate'
          git push origin HEAD:feature/long-capture-next-v12
